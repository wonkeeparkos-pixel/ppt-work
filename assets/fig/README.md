# 그림 소스

| 파일 | 쓰이는 곳 | 출처 |
|---|---|---|
| `mech_nmj.webp` | 03쪽 좌 — 신경근접합부 차단 | Higgsfield `nano_banana_pro` 생성 (2026-08-02), 라벨 없는 원화 |
| `mech_nociceptor.webp` | 03쪽 우 — 통각 종말 차단·역행성 수송 | 〃 |

두 그림 모두 **글자가 없는 상태로 생성**하고, 한글 라벨은 덱에서 Pretendard로 얹는다.
AI 이미지 생성기는 한글을 깨뜨리므로 그림 안에 글자를 넣지 않는다.
`build_botox_v1.py`가 이 파일들을 base64 data URI로 HTML에 임베드한다.
